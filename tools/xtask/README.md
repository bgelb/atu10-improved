# xtask

`cargo xtask` is the repo's build, test, format, and environment-check entry
point. The alias is defined in `.cargo/config.toml`, so contributors do not
need a separately installed `cargo-xtask` binary.
